name: Native dependencies
description: Installs the system libraries the workspace builds and links against.

runs:
  using: composite
  steps:
    - name: Install native dependencies
      shell: bash
      run: |
        sudo apt-get update
        sudo apt-get install -y --no-install-recommends libclang-dev libeccodes-dev pkg-config
